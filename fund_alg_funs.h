#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>
#include <iterator>
#include "is_stable_funs.h"

					
std::vector<std::vector<int>> fund_alg(std::unordered_map<int,std::vector<int>>,
					std::unordered_map<int,std::vector<int>>);
